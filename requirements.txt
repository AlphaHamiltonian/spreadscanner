# Core networking dependencies
requests>=2.25.1
websocket-client>=1.2.3  # For synchronous WebSocket connections
websockets>=10.0  # For async WebSocket server

# Performance-optimized JSON parsing
orjson>=3.8.0  # Fast JSON serialization/deserialization

# GUI dependencies
# Note: tkinter comes with Python installation
# but on Linux may need to be installed separately with:
# sudo apt-get install python3-tk (Debian/Ubuntu)
# sudo yum install python3-tkinter (RHEL/CentOS)

# Development and build tools (optional)
# Only needed if compiling with Nuitka
# nuitka>=1.0.0  # For compiling to binary
# ordered-set>=4.1.0  # Required by Nuitka